diff --git a/index.html b/index.html
index 8f31c46..c67be48 100644
--- a/index.html
+++ b/index.html
@@ -26,7 +26,7 @@
 -->
 <link rel="preconnect" href="https://fonts.googleapis.com">
 <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;500&amp;family=IBM+Plex+Sans+KR:wght@400;500;600&amp;family=Noto+Sans+KR:wght@400;500;700&amp;display=swap" rel="stylesheet">
-  <link rel="stylesheet" href="style.css?v=20260905-13">
+  <link rel="stylesheet" href="style.css?v=20260905-14">
 </head>
 <body>
 
@@ -526,7 +526,7 @@
       <button aria-pressed="false" data-filter="achievement" data-kr="연구업적" data-en="Research Outputs">연구업적</button>
       <button aria-pressed="false" data-filter="notice" data-kr="공지" data-en="Notices">공지</button>
     </div>
-    <ul class="news" id="news-list" data-source="news.json?v=20260905-1" aria-live="polite">
+    <ul class="news" id="news-list" data-source="news.json?v=20260905-1" data-image-source="news-images.json?v=20260905-1" aria-live="polite">
       <li class="news-status" data-kr="소식을 불러오는 중입니다." data-en="Loading news.">소식을 불러오는 중입니다.</li>
     </ul>
     <div class="news-more-row">
@@ -564,6 +564,6 @@
   </div>
 </footer>
   
-  <script src="script.js?v=20260905-13"></script>
+  <script src="script.js?v=20260905-14"></script>
 </body>
 </html>
diff --git a/news-images.json b/news-images.json
new file mode 100644
index 0000000..3d58231
--- /dev/null
+++ b/news-images.json
@@ -0,0 +1,150 @@
+{
+ "1252": [
+  "2026년_2학기_전기전자_종합설계_주제_안내.png"
+ ],
+ "1246": [
+  "2026년_5월_23일_안산_자락길_산책을_성공적으로_마쳤습니다.jpg"
+ ],
+ "1234": [
+  "2026년_3월_8일_부천국제_10km_로드레이스를_성공적으로_마쳤습니다_1.jpg",
+  "2026년_3월_8일_부천국제_10km_로드레이스를_성공적으로_마쳤습니다_2.jpg"
+ ],
+ "1223": [
+  "2026년_2월_학위수여식_-_김병수,_정래상,_조한성_졸업_1.jpg",
+  "2026년_2월_학위수여식_-_김병수,_정래상,_조한성_졸업_2.jpg",
+  "2026년_2월_학위수여식_-_김병수,_정래상,_조한성_졸업_3.jpg"
+ ],
+ "1212": [
+  "2026년_1학기_전기전자_종합설계_주제_안내.png"
+ ],
+ "1205": [
+  "2025_테스트_기술_워크샵_성료_1.jpg",
+  "2025_테스트_기술_워크샵_성료_2.jpg"
+ ],
+ "1198": [
+  "2025년_8월_학위수여식_-_김희태,_한동현,_이상준,_정화영_졸업_1.jpg",
+  "2025년_8월_학위수여식_-_김희태,_한동현,_이상준,_정화영_졸업_2.jpg"
+ ],
+ "1193": [
+  "2025년도_MT를_성공적으로_마쳤습니다_1.jpg",
+  "2025년도_MT를_성공적으로_마쳤습니다_2.jpg",
+  "2025년도_MT를_성공적으로_마쳤습니다_3.jpg"
+ ],
+ "1181": [
+  "2025년_2학기_전기전자_종합설계_주제_안내.png"
+ ],
+ "1163": [
+  "2025년_2월_학위수여식_-_이영광,_장석준,_윤준식,_정재영_졸업.jpg"
+ ],
+ "1150": [
+  "2025년_1학기_전기전자_종합설계_주제_안내.png"
+ ],
+ "1140": [
+  "2024년_8월_학위수여식_-_김태현,_임현찬,_김혜민_졸업.jpg"
+ ],
+ "1126": [
+  "2024년_2학기_전기전자_종합설계_주제_안내.png"
+ ],
+ "1109": [
+  "2024년_1학기_전기_전자_종합_설계_주제_안내_1.png",
+  "2024년_1학기_전기_전자_종합_설계_주제_안내_2.png",
+  "2024년_1학기_전기_전자_종합_설계_주제_안내_3.png"
+ ],
+ "1098": [
+  "2023년_8월_학위수여식_-_이하영,_정민호,_박성환_졸업.jpg"
+ ],
+ "1082": [
+  "2022년도_송년회가_잘_마무리_되었습니다_1.jpg",
+  "2022년도_송년회가_잘_마무리_되었습니다_2.jpg"
+ ],
+ "1068": [
+  "2022년_8월_학위수여식_-_이용,_김경빈,_이인환_졸업_1.jpg",
+  "2022년_8월_학위수여식_-_이용,_김경빈,_이인환_졸업_2.jpg"
+ ],
+ "1063": [
+  "2022년_2학기_전기_전자_종합_설계_주제_안내.jpg"
+ ],
+ "1047": [
+  "2022년_2월_학위수여식_-_김지혜,_이권형,_우형일,_김호경_졸업_1.jpg",
+  "2022년_2월_학위수여식_-_김지혜,_이권형,_우형일,_김호경_졸업_2.jpg"
+ ],
+ "1048": [
+  "2022년_1학기_전기_전자_종합_설계_주제_안내.png"
+ ],
+ "1044": [
+  "2021년도_송년회가_잘_마무리_되었습니다_1.jpg",
+  "2021년도_송년회가_잘_마무리_되었습니다_2.jpg"
+ ],
+ "1028": [
+  "2021년_2학기_전기_전자_종합_설계_주제_안내.png"
+ ],
+ "1023": [
+  "자동차_시스템_반도체_융합_전문_인력_육성_사업_선정.png"
+ ],
+ "1008": [
+  "2021년_1학기_전기_전자_종합_설계_주제_안내.png"
+ ],
+ "1000": [
+  "2020년도_송년회가_잘_마무리_되었습니다.jpg"
+ ],
+ "995": [
+  "2020년_2학기_전기_전자_종합_설계_주제_안내.jpg"
+ ],
+ "976": [
+  "2020년_2월_학위수여식_-_이영우,_오형교_졸업.jpg"
+ ],
+ "946": [
+  "2019년도_송년_워크샵이_잘_마무리_되었습니다_1.jpg",
+  "2019년도_송년_워크샵이_잘_마무리_되었습니다_2.jpg"
+ ],
+ "926": [
+  "2019년_SK하이닉스_반도체_혁신_아이디어_공모전_최우수상_수상_1.jpg",
+  "2019년_SK하이닉스_반도체_혁신_아이디어_공모전_최우수상_수상_2.jpg",
+  "2019년_SK하이닉스_반도체_혁신_아이디어_공모전_최우수상_수상_3.jpg"
+ ],
+ "924": [
+  "2019년_10월_30일_EE-Festival을_성공적으로_마쳤습니다_1.jpg",
+  "2019년_10월_30일_EE-Festival을_성공적으로_마쳤습니다_2.jpg",
+  "2019년_10월_30일_EE-Festival을_성공적으로_마쳤습니다_3.jpg"
+ ],
+ "923": [
+  "2019_Test_Technology_Workshop_1.jpg",
+  "2019_Test_Technology_Workshop_2.jpg"
+ ],
+ "875": [
+  "2019년도_MT를_성공적으로_마쳤습니다.jpg"
+ ],
+ "872": [
+  "2019년_5월_18일_체육대회를_성공적으로_마쳤습니다.jpg"
+ ],
+ "847": [
+  "2019년_2월_학위수여식_-_이인걸,_서성열,_이동수_졸업.jpg"
+ ],
+ "768": [
+  "2018년_8월_학위수여식_-_최인혁_졸업.jpg"
+ ],
+ "675": [
+  "2017년_12월_인공지능_빅데이터를_활용한_3차원_반도체_신뢰성_및_수율_향상_기술_워크샵.jpg"
+ ],
+ "614": [
+  "2016년_12월_3D_IC_신뢰성_및_수율_향상_기술_워크샵.jpg"
+ ],
+ "323": [
+  "2015년_5월_9일_체육대회를_성공적으로_마쳤습니다.jpg"
+ ],
+ "318": [
+  "체육대회_점심식사_및_뒤풀이_공지_1.jpg",
+  "체육대회_점심식사_및_뒤풀이_공지_2.png"
+ ],
+ "274": [
+  "2014년도_2학기_졸업을_축하합니다.jpg"
+ ],
+ "259": [
+  "2014년도_2학기_MT를_성공적으로_마쳤습니다_1.jpg",
+  "2014년도_2학기_MT를_성공적으로_마쳤습니다_2.jpg"
+ ],
+ "54": [
+  "2014년도_1학기_졸업을_축하합니다_1.jpg",
+  "2014년도_1학기_졸업을_축하합니다_2.jpg"
+ ]
+}
\ No newline at end of file
diff --git a/script.js b/script.js
index 0ce06b8..1e1899a 100644
--- a/script.js
+++ b/script.js
@@ -426,7 +426,11 @@ function closeCurrentMemberDetails(exceptButton = null) {
   });
 }
 
-function placeDetailAfterCardRow(card, detail) {
+function placeDetailAfterCardRow(
+  card,
+  detail,
+  cardSelector = ".member-card, .alumni-card"
+) {
   const container = card.parentElement;
   if (!container) {
     return;
@@ -437,7 +441,7 @@ function placeDetailAfterCardRow(card, detail) {
   const cardTop = card.offsetTop;
   const rowCards = [...container.children].filter(
     (element) =>
-      element.matches(".member-card, .alumni-card") &&
+      element.matches(cardSelector) &&
       Math.abs(element.offsetTop - cardTop) <= 1
   );
   const lastCardInRow = rowCards[rowCards.length - 1] || card;
@@ -464,9 +468,10 @@ function repositionOpenMemberDetails() {
 let memberDetailResizeFrame = 0;
 window.addEventListener("resize", () => {
   window.cancelAnimationFrame(memberDetailResizeFrame);
-  memberDetailResizeFrame = window.requestAnimationFrame(
-    repositionOpenMemberDetails
-  );
+  memberDetailResizeFrame = window.requestAnimationFrame(() => {
+    repositionOpenMemberDetails();
+    repositionOpenNewsDetail();
+  });
 });
 
 function createCurrentMemberElements(member, index) {
@@ -1355,6 +1360,8 @@ async function initializeProjects() {
 
 const NEWS_PAGE_SIZE = 9;
 
+const NEWS_IMAGE_BASE = "images/notice/";
+
 const NEWS_BOARDS = {
   publication: "achievement",
   patent: "achievement",
@@ -1376,8 +1383,10 @@ const NEWS_CATEGORY_LABELS = {
 
 const newsState = {
   items: [],
+  imagesById: {},
   board: "all",
   visibleCount: NEWS_PAGE_SIZE,
+  openId: null,
 };
 
 function getNewsFilterCategory(category) {
@@ -1413,6 +1422,30 @@ function sortNewsItems(items) {
   });
 }
 
+function getNewsImages(item) {
+  const files = newsState.imagesById[String(item.id)];
+  return Array.isArray(files) ? files : [];
+}
+
+function createNewsMeta(item) {
+  const meta = document.createElement("div");
+  meta.className = "news-meta";
+
+  const label = getNewsCategoryLabel(item.category);
+  const category = document.createElement("span");
+  category.className = "cat";
+  setLocalizedContent(category, label.kr, label.en);
+
+  const date = document.createElement("time");
+  if (typeof item.date === "string") {
+    date.dateTime = item.date;
+  }
+  date.textContent = formatNewsDate(item.date);
+
+  meta.append(category, date);
+  return meta;
+}
+
 function createNewsBody(body) {
   const fragment = document.createDocumentFragment();
 
@@ -1434,54 +1467,151 @@ function createNewsBody(body) {
   return fragment;
 }
 
-function createNewsCard(item) {
-  const card = document.createElement("li");
-  card.dataset.cat = typeof item.category === "string" ? item.category : "notice";
-  card.dataset.board = getNewsFilterCategory(item.category);
-
-  const label = getNewsCategoryLabel(item.category);
-  const category = document.createElement("span");
-  category.className = "cat";
-  setLocalizedContent(category, label.kr, label.en);
+function createNewsGallery(item) {
+  const files = getNewsImages(item);
+  if (!files.length) {
+    return null;
+  }
 
-  const date = document.createElement("time");
-  if (typeof item.date === "string") {
-    date.dateTime = item.date;
+  const gallery = document.createElement("div");
+  gallery.className = "news-gallery";
+  if (files.length === 1) {
+    gallery.classList.add("news-gallery-single");
   }
-  date.textContent = formatNewsDate(item.date);
 
-  const meta = document.createElement("div");
-  meta.className = "news-meta";
-  meta.append(category, date);
+  files.forEach((file, index) => {
+    const source = NEWS_IMAGE_BASE + encodeURIComponent(file);
+    const link = document.createElement("a");
+    link.className = "news-gallery-item";
+    link.href = source;
+    link.target = "_blank";
+    link.rel = "noopener";
+
+    const image = document.createElement("img");
+    image.src = source;
+    image.loading = "lazy";
+    image.decoding = "async";
+    image.alt =
+      (typeof item.title === "string" ? item.title : "") +
+      (files.length > 1 ? " (" + (index + 1) + ")" : "");
+
+    link.append(image);
+    gallery.append(link);
+  });
+
+  return gallery;
+}
+
+function createNewsDetailPanel(item, detailId) {
+  const panel = document.createElement("li");
+  panel.className = "news-detail-panel";
+  panel.id = detailId;
+  panel.hidden = true;
 
   const title = document.createElement("h3");
+  title.className = "news-detail-title";
   title.textContent = typeof item.title === "string" ? item.title : "";
 
-  card.append(meta, title);
+  panel.append(createNewsMeta(item), title);
 
   const body = createNewsBody(item.body);
-  if (!body.childNodes.length) {
-    return card;
+  if (body.childNodes.length) {
+    const bodyContainer = document.createElement("div");
+    bodyContainer.className = "news-detail-body";
+    bodyContainer.append(body);
+    panel.append(bodyContainer);
   }
 
-  const panel = document.createElement("details");
-  panel.className = "news-detail";
+  const gallery = createNewsGallery(item);
+  if (gallery) {
+    panel.append(gallery);
+  }
 
-  const summary = document.createElement("summary");
-  const summaryText = document.createElement("span");
-  setLocalizedContent(summaryText, "내용 보기", "Read more");
-  const summaryIcon = document.createElement("span");
-  summaryIcon.className = "summary-icon";
-  summaryIcon.setAttribute("aria-hidden", "true");
-  summary.append(summaryText, summaryIcon);
-
-  const bodyContainer = document.createElement("div");
-  bodyContainer.className = "news-detail-body";
-  bodyContainer.append(body);
-
-  panel.append(summary, bodyContainer);
-  card.append(panel);
-  return card;
+  return panel;
+}
+
+function closeOpenNewsDetail() {
+  newsState.openId = null;
+
+  document
+    .querySelectorAll('.news-card-button[aria-expanded="true"]')
+    .forEach((button) => {
+      button.setAttribute("aria-expanded", "false");
+      const panel = document.getElementById(button.getAttribute("aria-controls"));
+      if (panel) {
+        panel.hidden = true;
+      }
+    });
+}
+
+function repositionOpenNewsDetail() {
+  document
+    .querySelectorAll('.news-card-button[aria-expanded="true"]')
+    .forEach((button) => {
+      const card = button.closest(".news-card");
+      const panel = document.getElementById(button.getAttribute("aria-controls"));
+
+      if (card && panel) {
+        placeDetailAfterCardRow(card, panel, ".news-card");
+      }
+    });
+}
+
+function createNewsCard(item, index) {
+  const card = document.createElement("li");
+  card.className = "news-card";
+  card.dataset.cat = typeof item.category === "string" ? item.category : "notice";
+  card.dataset.board = getNewsFilterCategory(item.category);
+
+  const title = document.createElement("h3");
+  title.textContent = typeof item.title === "string" ? item.title : "";
+
+  const hasBody = Boolean(String(item.body ?? "").trim());
+  const hasImages = getNewsImages(item).length > 0;
+
+  if (!hasBody && !hasImages) {
+    const static_ = document.createElement("div");
+    static_.className = "news-card-static";
+    static_.append(createNewsMeta(item), title);
+    card.append(static_);
+    return { card, panel: null };
+  }
+
+  const detailId = "news-detail-" + String(item.id ?? index);
+  const button = document.createElement("button");
+  button.className = "news-card-button";
+  button.type = "button";
+  button.setAttribute("aria-expanded", "false");
+  button.setAttribute("aria-controls", detailId);
+
+  const toggle = document.createElement("span");
+  toggle.className = "news-card-toggle";
+  const toggleText = document.createElement("span");
+  setLocalizedContent(toggleText, "내용 보기", "Read more");
+  const toggleIcon = document.createElement("span");
+  toggleIcon.className = "summary-icon";
+  toggleIcon.setAttribute("aria-hidden", "true");
+  toggle.append(toggleText, toggleIcon);
+
+  button.append(createNewsMeta(item), title, toggle);
+  card.append(button);
+
+  const panel = createNewsDetailPanel(item, detailId);
+
+  button.addEventListener("click", () => {
+    const willOpen = panel.hidden;
+    closeOpenNewsDetail();
+
+    if (willOpen) {
+      placeDetailAfterCardRow(card, panel, ".news-card");
+      newsState.openId = item.id;
+    }
+
+    panel.hidden = !willOpen;
+    button.setAttribute("aria-expanded", String(willOpen));
+  });
+
+  return { card, panel };
 }
 
 function getVisibleNewsItems() {
@@ -1500,11 +1630,21 @@ function renderNewsList() {
     return;
   }
 
+  newsState.openId = null;
+
   const items = getVisibleNewsItems();
   const visible = items.slice(0, newsState.visibleCount);
 
   if (visible.length) {
-    list.replaceChildren(...visible.map(createNewsCard));
+    const nodes = [];
+    visible.forEach((item, index) => {
+      const parts = createNewsCard(item, index);
+      nodes.push(parts.card);
+      if (parts.panel) {
+        nodes.push(parts.panel);
+      }
+    });
+    list.replaceChildren(...nodes);
   } else {
     const empty = document.createElement("li");
     empty.className = "news-status";
@@ -1563,12 +1703,47 @@ function initializeNewsFilters() {
   const moreButton = document.getElementById("news-more");
   if (moreButton) {
     moreButton.addEventListener("click", () => {
+      const openButton = document.querySelector(
+        '.news-card-button[aria-expanded="true"]'
+      );
+      const openId = openButton
+        ? openButton.getAttribute("aria-controls")
+        : null;
+
       newsState.visibleCount += NEWS_PAGE_SIZE;
       renderNewsList();
+
+      if (openId) {
+        const restored = document.querySelector(
+          '.news-card-button[aria-controls="' + openId + '"]'
+        );
+        if (restored) {
+          restored.click();
+        }
+      }
     });
   }
 }
 
+async function loadNewsImageMap(source) {
+  if (!source) {
+    return {};
+  }
+
+  try {
+    const response = await fetch(source, { cache: "no-cache" });
+    if (!response.ok) {
+      throw new Error("News image map request failed: " + response.status);
+    }
+
+    const map = await response.json();
+    return map && typeof map === "object" ? map : {};
+  } catch (error) {
+    console.error(error);
+    return {};
+  }
+}
+
 async function initializeNews() {
   const list = document.getElementById("news-list");
   if (!list) {
@@ -1578,7 +1753,10 @@ async function initializeNews() {
   initializeNewsFilters();
 
   try {
-    const response = await fetch(list.dataset.source, { cache: "no-cache" });
+    const [response, imageMap] = await Promise.all([
+      fetch(list.dataset.source, { cache: "no-cache" }),
+      loadNewsImageMap(list.dataset.imageSource),
+    ]);
 
     if (!response.ok) {
       throw new Error("News data request failed: " + response.status);
@@ -1589,6 +1767,7 @@ async function initializeNews() {
       throw new TypeError("News data must be an array.");
     }
 
+    newsState.imagesById = imageMap;
     newsState.items = sortNewsItems(items);
     renderNewsList();
   } catch (error) {
diff --git a/style.css b/style.css
index 5cc1799..36a7913 100644
--- a/style.css
+++ b/style.css
@@ -805,27 +805,82 @@ h2::after{content:"";display:block;width:40px;height:3px;background:var(--line-m
 .news time{font-size:13px;color:var(--ink-soft);margin-left:8px}
 .news h3{font-size:16.5px;font-weight:500;margin:8px 0 6px}
 .news p{font-size:14.5px;color:var(--ink-soft)}
-.news li{display:flex;flex-direction:column}
+.news li{display:flex;flex-direction:column;padding:0}
+.news li.news-card>*{padding:22px}
 .news .news-meta{display:flex;align-items:baseline;flex-wrap:wrap}
-.news .news-detail{margin-top:auto;padding-top:10px}
-.news .news-detail>summary{
+.news-card-button{
+  display:flex;
+  flex-direction:column;
+  flex:1;
+  width:100%;
+  font:inherit;
+  color:inherit;
+  text-align:left;
+  background:none;
+  border:0;
+  border-radius:6px;
+  cursor:pointer;
+}
+.news-card-button h3{margin:8px 0 6px}
+.news-card-toggle{
   display:flex;
   align-items:center;
   justify-content:space-between;
   gap:10px;
-  cursor:pointer;
-  list-style:none;
+  margin-top:auto;
+  padding-top:12px;
+  border-top:1px solid var(--line);
   font-size:13.5px;
   font-weight:500;
   color:var(--copper);
-  padding-top:12px;
-  border-top:1px solid var(--line);
 }
-.news .news-detail>summary::-webkit-details-marker{display:none}
-.news .news-detail[open]>summary .summary-icon::after{transform:translate(-50%,-50%) rotate(0)}
-.news-detail-body{padding-top:10px}
-.news-detail-body p{margin:0 0 10px;white-space:normal;word-break:break-word}
+.news-card-static{display:flex;flex-direction:column}
+.news-card-static h3{margin:8px 0 6px}
+.news-card-button:hover,
+.news-card-button[aria-expanded="true"]{background:var(--bg-soft)}
+.news-card-button:hover .news-card-toggle{color:var(--copper-lt)}
+.news-card-button[aria-expanded="true"] .summary-icon::after{
+  transform:translate(-50%,-50%) rotate(0);
+}
+
+.news-detail-panel{
+  grid-column:1/-1;
+  display:block;
+  padding:26px 28px;
+  border:1px solid var(--line);
+  border-radius:10px;
+  background:#fff;
+  box-shadow:0 8px 26px rgba(58,99,137,.08);
+  text-align:left;
+}
+.news-detail-panel[hidden]{display:none}
+.news-detail-title{font-size:19px;font-weight:500;margin:8px 0 14px;line-height:1.45}
+.news-detail-body{color:var(--ink-soft);font-size:14.5px;line-height:1.75}
+.news-detail-body p{margin:0 0 12px;word-break:break-word}
 .news-detail-body p:last-child{margin-bottom:0}
+.news-gallery{
+  display:grid;
+  grid-template-columns:repeat(auto-fit,minmax(240px,1fr));
+  gap:14px;
+  margin-top:20px;
+}
+.news-gallery-single{grid-template-columns:minmax(0,560px)}
+.news-gallery-item{
+  display:flex;
+  align-items:center;
+  justify-content:center;
+  overflow:hidden;
+  border:1px solid var(--line);
+  border-radius:7px;
+  background:var(--bg-soft);
+}
+.news-gallery-item img{
+  display:block;
+  max-width:100%;
+  max-height:440px;
+  width:auto;
+  height:auto;
+}
 .news-status{font-size:14.5px;color:var(--ink-soft)}
 .news li.news-status{
   grid-column:1/-1;
@@ -1542,7 +1597,7 @@ html[lang="en"] .member-detail-name .member-name-english{
   padding:18px 20px;
   border:1px solid var(--line);
   border-radius:7px;
-  background:var(--bg-soft);
+  background:#fff;
 }
 
 .professor-awards-section{
@@ -1814,6 +1869,14 @@ html[lang="en"] .alumni-card-name .member-name-english{
     padding:20px;
   }
 
+  .news-detail-panel{
+    padding:20px;
+  }
+
+  .news-gallery{
+    grid-template-columns:1fr;
+  }
+
   .member-profile-field{
     grid-template-columns:1fr;
     gap:3px;
